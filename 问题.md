## vue2 3的diff算法对比

    Vue2 是“双端指针 + 暴力对比”，Vue3 是“最长递增子序列（LIS）优化的最小移动策略”。
    Vue2 采用的是 双端对比算法（双指针）： 同时从头尾向中间比较：
    Vue3 在 key diff 时引入了： 最长递增子序列（LIS）
    步骤：先做头尾对比（和 Vue2 一样）
        中间乱序部分：
            建立 key → index 映射
            找出哪些节点需要移动
            计算 最长递增子序列（LIS）
            只移动不在 LIS 里的节点
    只移动必要节点（最少 DOM 操作）
    时间复杂度更优（尤其大列表）
    Vue2 使用双端指针进行 diff，在处理中间乱序时可能产生较多无效移动；Vue3 在此基础上引入最长递增子序列算法，只移动必要节点，从而减少 DOM 操作。同时配合编译阶段的 PatchFlag 和 Block Tree 优化，使整体性能更高。

## 如果有很多个银行机构数据，怎么处理

    1.分治：按机构拆数据
    2.虚拟列表（大数据必备）
    3.分页 / 懒加载（树结构重点）
    4.key 设计（直接影响 diff🔥）
    5.避免无效响应式（Vue3关键）
    6.分块更新（避免卡顿）

## 首页加载白屏很慢，怎么处理，给出思路

    - 思路：测 → 定位 → 分层优化 → 验证
    - 1.使用 Chrome DevTools 的 Performance（性能） 面板记录首页加载的各个阶段，找出耗时较长的操作。查看FCP（首次绘制）| LCP（最大内容绘制） 首屏核心TTI |（可交互时间）
    - 2.定位到耗时较长的操作后，分析原因，可能是资源加载、渲染、数据请求等。
    - 3.针对不同的原因，采取相应的优化措施，如：
        - 资源加载：使用 CDN、压缩资源、按需加载等。
        - 渲染：使用虚拟列表、懒加载、分块更新等。
        - 数据请求：使用缓存、减少请求次数、优化请求速度等。
        具体而言
        1. js体积过大，分包，按需加载
        2. 主线程被阻塞（解析 + 执行 JS）拆分任务（chunk）|使用 requestIdleCallback | Web Worker（复杂计算）
        3. 请求过多 / 串行请求
        4. 资源加载顺序不合理 js阻塞html，defer异步加载
        5. 首屏渲染内容太多 skeleton 骨架屏|分页 / 懒加载

## spa应用和普通网站的区别

| 对比点   | SPA               | 传统网站（MPA）    |
| -------- | ----------------- | ------------------ |
| 页面加载 | 首次加载一次      | 每次跳转都加载     |
| 页面跳转 | JS 控制（无刷新） | 浏览器跳转（刷新） |
| 渲染方式 | 前端渲染（CSR）   | 服务端渲染（SSR）  |
| URL 变化 | history API       | 直接请求新页面     |
| 用户体验 | 流畅              | 有白屏             |

    - SPA 核心技术
        路由控制（history.pushState）
        虚拟 DOM（Vue / React）
        前后端分离（API）
    - SPA 优缺点
        - 体验好（无刷新）| 前后端解耦| 组件化开发
        - 首屏慢（关键）| SEO 差 | 首次加载资源大

## qiankun框架

- Qiankun 是一个基于微前端思想的框架，可以把多个前端应用组合在一起运行，同时保证它们之间互不干扰。
- 核心底层原理
  - HTML Entry（核心入口机制）
    - 加载子应用 HTML → 解析 → 提取 JS / CSS → 执行
  - JS 沙箱（核心隔离🔥）
    - Proxy 代理 window
  - CSS 隔离
    - scoped（样式重写）
    - Shadow DOM
  - 路由劫持（来自 single-spa）
- 整体运行流程
  1. 主应用监听路由变化
  2. 匹配子应用
  3. fetch 子应用 HTML（HTML Entry）
  4. 解析资源（JS / CSS）
  5. 创建沙箱（Proxy）
  6. 执行 bootstrap
  7. 执行 mount（渲染）
  8. 切换路由 → unmount（销毁）
- 总结：Qiankun 并不是基于 iframe，它是通过 HTML Entry 加载子应用资源，并通过 Proxy 实现 JS 沙箱，通过样式隔离解决 CSS 冲突，再结合 single-spa 的路由劫持和生命周期管理，实现多个应用在同一页面下的隔离运行。
