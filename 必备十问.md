# 银行前端 17k 面试必背 10 问（Vue2 主）

> 适用：银行 / 金融 / 政企后台系统（Vue2 + Element UI）
> 用途：**直接背 + 直接说**，不拼源码，拼稳定度

---

## 1️⃣ Vue2 响应式原理（必背）

Vue2 通过 `Object.defineProperty` 劫持对象属性，在 **get 时收集依赖，在 set 时触发更新**。初始化阶段会递归劫持对象的每个属性，因此存在性能成本。

缺点包括：

* 无法监听新增/删除属性
* 数组索引修改、length 修改无法触发更新
* 需要大量 hack（如重写数组方法）

解决方案是：**提前在 data 中声明属性，或使用 `Vue.set` 保证响应式。**

---

## 2️⃣ 为什么 Vue2 需要 Vue.set？

因为 Vue2 只能劫持初始化时存在的属性，**后续新增的属性没有 getter / setter**，Vue 无法追踪依赖。

`Vue.set` 本质是：

* 为新属性手动创建响应式
* 主动触发依赖更新

在银行项目中，应优先在 data 中定义完整结构，避免运行期频繁新增属性。

---

## 3️⃣ computed 和 watch 的核心区别

* **computed**：用于派生状态，本质是 **带缓存的 watcher**，依赖不变不会重新计算
* **watch**：用于监听变化并执行副作用，适合异步、联动、请求等场景

原则：

> 算一个值 → computed
> 监听变化做事 → watch

---

## 4️⃣ Vue3 相比 Vue2 的核心改进

Vue3 使用 **Proxy 代理对象的访问行为**，而不是劫持属性本身：

* 无需递归劫持，按需触发
* 支持新增 / 删除属性
* 数组无需 hack
* 性能和可维护性更好

银行场景需注意：**Proxy 不支持 IE，升级必须考虑浏览器环境和回退方案。**

---

## 5️⃣ ref 和 reactive 如何选择

* 基础类型：`ref`
* 对象/数组：`reactive`

注意点：

* reactive 解构会丢失响应式
* 模板中 ref 会自动解包 `.value`

实际项目中以 **可读性和一致性优先**。

---

## 6️⃣ 标准查询 + 表格请求流程（必背模板）

1. 点击查询 → `loading = true`
2. 禁用按钮 / 防抖，防止重复请求
3. 发起接口请求
4. 成功：

   * 校验状态码
   * 清空旧数据，避免脏数据
   * 赋值 tableData
   * 更新分页信息
5. 失败：

   * 统一错误提示
   * 数据兜底为空
6. finally：`loading = false`

---

## 7️⃣ Element UI 表单校验设计

* 使用 `rules` + `trigger` 配置校验
* 提交前统一 `this.$refs.form.validate`
* 动态表单：

  * 新增后 `nextTick + clearValidate`
  * 删除时同步移除校验逻辑

复杂校验应抽成独立方法，避免 rules 过重。

---

## 8️⃣ 大表格性能优化方案

优先级：

1. 分页（最优）
2. 虚拟滚动（必须全量时）
3. 懒加载 / 按需渲染

避免：

* 深度 watch 大对象
* 每次更新整表重渲

高频更新场景：通过 computed diff，只更新必要数据。

---

## 9️⃣ 通用列表页组件设计思路

拆分为：

* 查询区（配置驱动）
* 表格区（columns schema + slot 扩展）
* 分页区（受控组件）
* 请求层（统一封装）
* 权限控制（按钮 / 列级）

统一 loading、空态和错误兜底，提升复用性。

---

## 🔟 银行项目的工程化与稳定性意识（加分项）

* 明确浏览器和终端环境
* 灰度发布 / 网点试点
* 保留稳定回退版本
* 新功能不能影响存量功能

> 在银行项目中，**稳定性永远高于炫技。**

---

✅ 建议：打印 / 收藏 / 面试前过 2 遍
